{
  "name": "@chude/memory",
  "version": "2.0.0",
  "description": "Cross-project context persistence for Claude Code sessions",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "memory": "dist/presentation/cli/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build:types": "tsc --project tsconfig.lib.json || true",
    "build:lib": "bun build src/index.ts --outdir dist --target bun --external onnxruntime-node --external bun:sqlite --external @anthropic-ai/claude-code --external @huggingface/transformers --external sqlite-vec --external commander --external cli-progress --external chrono-node --external fuzzy --external @inquirer/search --external @inquirer/select --external @gutenye/commander-completion-carapace",
    "build:cli": "bun build src/presentation/cli/index.ts --outdir dist/presentation/cli --target bun --external onnxruntime-node",
    "build": "bun run build:types && bun run build:lib && bun run build:cli",
    "build:hook": "bun build src/infrastructure/hooks/sync-hook-script.ts --outfile=dist/sync-hook.js --target=bun",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "test:smoke": "bun test tests/smoke",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "mutation": "stryker run",
    "mutation:domain": "stryker run --mutate 'src/domain/**/!(*.test).ts'"
  },
  "author": "Chude <chude@emeke.org>",
  "license": "MIT",
  "devDependencies": {
    "@stryker-mutator/core": "^9.0.0",
    "@stryker-mutator/typescript-checker": "^9.0.0",
    "@stryker-mutator/vitest-runner": "^9.0.0",
    "@types/bun": "^1.1.0",
    "@types/cli-progress": "^3.11.6",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "claude",
    "claude-code",
    "context",
    "memory",
    "session",
    "search"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/user/memory-nexus"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^2.1.58",
    "@gutenye/commander-completion-carapace": "^1.0.9",
    "@huggingface/transformers": "^3.8.1",
    "@inquirer/search": "^4.1.0",
    "@inquirer/select": "^5.0.4",
    "chrono-node": "^2.9.0",
    "cli-progress": "^3.12.0",
    "commander": "^14.0.2",
    "fuzzy": "^0.1.3",
    "sqlite-vec": "0.1.6"
  }
}
