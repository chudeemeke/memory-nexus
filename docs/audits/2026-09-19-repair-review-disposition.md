# Baseline repair review disposition

Reviewer: live `fable` alias, high effort, after a separate READY smoke and matching runtime skill hashes. Scope: supplied code diff and new isolation test. The reviewer used no tools and did not independently inspect the repository, lockfile or dependencies. Verdict: **APPROVE WITH CONDITIONS**, not merge acceptance. Full text: `2026-09-19-repair-evidence/baseline-code-fable-review.md`.

| Condition | Evidence and disposition |
| --- | --- |
| C1: test-home lifecycle and truthful retention | The requested two-file ownership probe passed even before the repair; premature per-file teardown was not reproduced. A stronger child-process regression did reproduce a surviving SQLite directory with its owner marker missing (ENOENT). Cleanup now runs at process exit and restores the ownership record after a tolerated Windows lock failure. The focused ownership and hook tests pass, 20/20. No blind startup sweep was added: a directory name and dead PID alone are not sufficient authority to delete arbitrary stale folders. Existing unmarked leftovers remain inventoried. |
| C2: actual storage resolver and runner wiring | `src/infrastructure/paths.ts` uses XDG bases for database/config and `homedir()` for default/legacy/fallback content. `MEMORY_HOME` selects the legacy content directory; it does not enable legacy sync/backfill. Those commands separately require their explicit flag, `config.legacyMemoryFiles.enabled`, or `MEMORY_LEGACY_MEMORY_FILES=1`. The named APPDATA/LOCALAPPDATA/CLAUDE_CONFIG_DIR alternatives do not occur in production source. `bunfig.toml` loads the preload. `test:isolation` is a static source checker, not a child-per-file test runner. The instrumented coverage lane is Bun, not Vitest; a current-revision instrumented run is still pending. This environment isolation is not an OS sandbox against deliberately hardcoded paths. |
| C3: stronger negative proof | The inherited-home test now preserves a seeded `.claude/friction.jsonl` sentinel byte-for-byte, checks the inherited directory inventory (allowing Bun's own bootstrap `.bun` cache), verifies the generated test-home path, and verifies retained ownership against the exited child PID before removing that exact test-owned path. Its timeout exceeds the child timeout. The initial no-guard RED run demonstrated an actual database write in the inherited home; evidence is local `memory-store-guard-red-20260919.log`. A committed no-preload control and assertions for every possible installer/runtime surface remain outside this bounded regression; no universal filesystem-isolation claim is made. |
| C4: all HOME/USERPROFILE overrides | Inspected every matching test override. `programmatic-api`, `unified-status`, `query-primitive`, sync helpers and the health-checker default-path test already override XDG config/data as well as HOME/USERPROFILE. The hook subprocess omission was fixed. Git transport/syncer/remote CLI fixtures already provide an isolated Git home and XDG config; the inline literal real-home strings in `sanitizeGitEnvironment` are data-only input to a pure environment sanitizer. No additional HOME-only memory-store fixture was found by this inventory. |
| C5: dependency/runtime compatibility | Open before merge: lockfile/dependency-range review, clean Linux install, real local embedding compatibility and vector identity assessment, and consumer-install dependency audit. The installed working-tree audit is clean; root overrides do not establish that a downstream global install resolves the same patched tree. No golden vector, Linux install or final artifact compatibility is claimed. Dependency edits have their own commit for review/rollback. |

The reviewer endorsed the embedding setup reorder, model-change count correction, resolver bound, color isolation, and numeric-evidence validation. Other findings remain owned by memory-nexus before their applicable acceptance gate: actual provider dimension validation, disposal-error masking, atomic model-generation replacement, index identity/preprocessing, stricter coverage boundary tests and zero-denominator policy, and POSIX resolver evidence. No hypothetical reviewer assertion was promoted to a verified production defect without reproduction or source support.

The corrected cleanup/isolation changes postdate this packet review. They need final-revision independent review before merge. The broader per-file quality inventory, friction quarantine and hosted/assembled-diff gates remain open in `2026-09-19-baseline-repair-status.md`.
