---
phase: 13-package-rename
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - deprecation-stub/package.json
  - deprecation-stub/index.js
  - MIGRATION.md
  - README.md
  - CLAUDE.md
  - src/presentation/cli/commands/install.ts
  - src/presentation/cli/commands/install.test.ts
autonomous: true

must_haves:
  truths:
    - "Deprecation stub package exists as a self-contained directory ready for independent npm publish"
    - "Running the deprecation stub's binary prints a message directing users to @chude/memory and exits with code 1"
    - "MIGRATION.md exists with upgrade instructions for existing memory-nexus users"
    - "CLAUDE.md references 'memory' binary, not 'memory-nexus'"
    - "memory install command warns about stale memory-nexus hook references"
    - "External WoW rules file renamed from memory-nexus.md to memory.md with updated content"
    - "Global CLAUDE.md memory table references 'memory' not 'memory-nexus'"
    - "All ~1,966+ existing tests pass after all changes"
  artifacts:
    - path: "deprecation-stub/package.json"
      provides: "Deprecation stub npm package metadata"
      contains: "memory-nexus"
    - path: "deprecation-stub/index.js"
      provides: "Deprecation message script"
      contains: "@chude/memory"
    - path: "MIGRATION.md"
      provides: "Upgrade guide for existing users"
      contains: "bun add -g @chude/memory"
    - path: "README.md"
      provides: "Updated project README"
      contains: "@chude/memory"
  key_links:
    - from: "deprecation-stub/package.json"
      to: "deprecation-stub/index.js"
      via: "bin field"
      pattern: "memory-nexus.*index.js"
    - from: "src/presentation/cli/commands/install.ts"
      to: "settings.json hooks"
      via: "stale hook detection for memory-nexus references"
      pattern: "memory-nexus|stale|legacy"
---

<objective>
Create deprecation stub package, migration documentation, update CLAUDE.md and README, rename external WoW rules file, and add stale hook detection to the install command. This completes the package rename phase.

Purpose: Finalizes the rename by creating the artifacts users will encounter: the deprecation stub (what happens when someone installs the old package name), migration documentation (how to upgrade), updated project documentation, updated external WoW rules, and the "detect stale memory-nexus hooks" warning in the install command per the locked decision.

Output: deprecation-stub/ directory ready for npm publish, MIGRATION.md, updated README.md and CLAUDE.md, renamed WoW rules file, install command with stale hook warning.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-package-rename/13-CONTEXT.md
@.planning/phases/13-package-rename/13-RESEARCH.md
@.planning/phases/13-package-rename/13-01-SUMMARY.md
@.planning/phases/13-package-rename/13-02-SUMMARY.md
@CLAUDE.md
@README.md
@~/.claude/rules/memory-nexus.md
@~/.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deprecation stub and add stale hook detection to install command</name>
  <files>
    deprecation-stub/package.json
    deprecation-stub/index.js
    src/presentation/cli/commands/install.ts
    src/presentation/cli/commands/install.test.ts
  </files>
  <action>
    **Deprecation stub package:**

    Create a `deprecation-stub/` directory at the project root. This is a separate, self-contained npm package that will be published independently to the `memory-nexus` package name on npm.

    `deprecation-stub/package.json`:
    ```json
    {
      "name": "memory-nexus",
      "version": "0.2.0",
      "description": "DEPRECATED: Use @chude/memory instead",
      "bin": {
        "memory-nexus": "index.js"
      },
      "author": "Chude <chude@emeke.org>",
      "license": "MIT",
      "keywords": ["deprecated"]
    }
    ```

    `deprecation-stub/index.js`:
    ```javascript
    #!/usr/bin/env node
    console.error("memory-nexus has been renamed to @chude/memory");
    console.error("");
    console.error("  Uninstall old:  bun remove -g memory-nexus");
    console.error("  Install new:    bun add -g @chude/memory");
    console.error("  Usage:          memory <command>");
    console.error("");
    console.error("  https://www.npmjs.com/package/@chude/memory");
    process.exit(1);
    ```

    This stub is intentionally minimal per the locked decision: "Stub published once and left forever. One version, never updated. It's a signpost."

    The stub does NOT need tests (it is a static script with no logic). It does NOT need to be included in the main package's build or test pipeline.

    **Stale hook detection in install command:**

    Read `src/presentation/cli/commands/install.ts` to understand the current install flow.

    Per locked decision: "memory install detects and warns if stale memory-nexus hook references still exist."

    After the install command completes hook installation (or reports already installed), add a check:
    1. Load Claude Code settings.json
    2. Scan all hook entries for commands containing the string "memory-nexus" (the LEGACY_MARKER)
    3. If any found, print a warning to stderr: "Warning: Stale memory-nexus hook references detected in settings.json. Run 'memory uninstall' then 'memory install' to clean up."
    4. This detection should also work if the user runs `memory install` and hooks are already installed with the new marker -- still scan for any lingering old-marker hooks that might exist from a different config block

    Import from settings-manager: the `loadClaudeSettings` function (already available) and check hook commands for "memory-nexus" string.

    Update install.test.ts:
    - Add test: when settings contain hooks with "memory-nexus" in command, install warns about stale references
    - Add test: when settings contain only new "memory" hooks, no stale warning
    - Add test: when settings have no hooks, no stale warning
  </action>
  <verify>
    Verify `deprecation-stub/package.json` exists with `"name": "memory-nexus"`.
    Run `node deprecation-stub/index.js` -- should print deprecation message and exit with code 1.
    Run `bun test src/presentation/cli/commands/install.test.ts` -- all tests pass including new stale detection tests.
  </verify>
  <done>
    Deprecation stub directory exists at project root, ready for independent npm publish. Install command detects and warns about stale memory-nexus hook references. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update MIGRATION.md, README.md, CLAUDE.md, external WoW rules, and run final test verification</name>
  <files>
    MIGRATION.md
    README.md
    CLAUDE.md
  </files>
  <action>
    **MIGRATION.md:**

    Create `MIGRATION.md` at the project root. Per locked decision: "Separate MIGRATION.md for existing users with upgrade instructions."

    Content structure:
    - Title: "Migrating from memory-nexus to @chude/memory"
    - Overview: One paragraph explaining the rename
    - Quick upgrade steps:
      1. `bun remove -g memory-nexus`
      2. `bun add -g @chude/memory`
      3. `memory doctor` (verify installation)
    - Automatic migration: Explain that on first run, `memory` automatically detects and migrates data from `~/.memory-nexus/` to new XDG paths (`~/.config/memory/` and `~/.local/share/memory/`). One-line notice is printed. No manual action needed.
    - Hook migration: Explain that the auto-migration does a full re-install of hooks (removes old, writes new with `memory` binary). Additionally `memory install` warns about any stale `memory-nexus` references. Recommend running `memory uninstall && memory install` if stale hooks persist.
    - What changed: Brief table of old vs new (package name, binary name, config path, data path, error class)
    - Rollback: If migration fails, data stays at old paths and an error message provides manual migration steps
    - Keep it concise. This is a reference document for existing users, not a tutorial.

    **README.md:**

    Read the current README.md. Per locked decision: "README presents @chude/memory as a fresh product."

    Update:
    - Replace all references to `memory-nexus` with `@chude/memory` (for the package name) or `memory` (for the CLI command)
    - Update installation instructions: `bun add -g @chude/memory`
    - Update usage examples: `memory sync`, `memory search "query"`, etc.
    - Add a note at the bottom or a brief section: "Previously published as memory-nexus. See MIGRATION.md for upgrade instructions."
    - Update any path references from `~/.memory-nexus/` to `~/.config/memory/` and `~/.local/share/memory/`
    - Do NOT add emojis per WoW rules

    **CLAUDE.md (project-level):**

    Read the current CLAUDE.md. Update:
    - Project name/title references
    - Command examples from `memory-nexus` to `memory`
    - Path references from `~/.memory-nexus/` to XDG paths
    - Any references to the `memory-nexus` binary
    - Keep the structure and content of CLAUDE.md intact -- only update identity references
    - The "Planned Commands" section should show `memory sync`, `memory search`, etc.
    - The "Session Storage Reference" section paths should stay the same (they reference `~/.claude/projects/` which is a DIFFERENT tool's paths, not memory-nexus's paths)

    **External WoW rules file (RENAME-05 BLOCKER FIX):**

    Rename `~/.claude/rules/memory-nexus.md` to `~/.claude/rules/memory.md`. This is the global WoW rules file that instructs Claude to query memory across projects. Update its content:
    - Title: "Memory: Cross-Project Context" (was "Memory-Nexus: Cross-Project Context")
    - All command examples: `memory context <project-name>` (was `memory-nexus context <project-name>`)
    - All command table entries: `memory search`, `memory context`, `memory related`, `memory show`, `memory list`, `memory search --project` (were all `memory-nexus ...`)
    - Project names section: update `nexus` description from `(memory-nexus)` to `(@chude/memory)`
    - Notes section: `memory-nexus is read-only` -> `memory is read-only`, `memory-nexus install` -> `memory install`
    - Keep the same structure and intent, only update the binary/tool name references

    To perform the rename:
    1. Read `~/.claude/rules/memory-nexus.md`
    2. Write updated content to `~/.claude/rules/memory.md`
    3. Delete `~/.claude/rules/memory-nexus.md` using `rm`

    **Global CLAUDE.md update (RENAME-05 BLOCKER FIX):**

    Read `~/.claude/CLAUDE.md`. In the Quick Reference table, update the Memory row:
    - `memory-nexus` references -> `memory`
    - The rule file reference: `rules/memory-nexus.md` -> `rules/memory.md`
    - Any other references to `memory-nexus` in the global CLAUDE.md

    **Final verification:**

    After all documentation updates, run the full test suite one final time:
    - `bun test` -- all tests pass
    - `bun run typecheck` -- confirm no new errors introduced by Phase 13 changes
    - Verify zero remaining references to "memory-nexus" in src/ production code (test data project names are acceptable)

    Grep verification commands to run:
    - `grep -rn "memory-nexus" src/ --include='*.ts' -l` -- only test files with project name test data should appear
    - `grep -rn "MemoryNexus" src/ --include='*.ts'` -- zero results
    - `grep -rn "MEMORY_NEXUS" src/ --include='*.ts'` -- zero results (LEGACY_MARKER in settings-manager.ts uses the string value, not the constant name)
  </action>
  <verify>
    Verify MIGRATION.md exists with upgrade instructions.
    Verify README.md references @chude/memory and memory binary.
    Verify CLAUDE.md references memory binary and new paths.
    Verify `~/.claude/rules/memory.md` exists with updated content referencing `memory` commands.
    Verify `~/.claude/rules/memory-nexus.md` does NOT exist (was renamed).
    Verify `~/.claude/CLAUDE.md` references `rules/memory.md` not `rules/memory-nexus.md`.
    Run `bun test` -- all tests pass.
    Run `bun run typecheck` -- no TypeScript errors.
    Grep confirms no stale identity references in production source code.
  </verify>
  <done>
    MIGRATION.md created with complete upgrade guide. README.md presents @chude/memory as the package identity. CLAUDE.md references updated. External WoW rules file renamed from memory-nexus.md to memory.md with all command references updated. Global CLAUDE.md updated to reference rules/memory.md. Full test suite passes. No new TypeScript errors introduced. No stale memory-nexus identity references in production code. Phase 13 complete.
  </done>
</task>

</tasks>

<verification>
1. `bun test` -- all tests pass (final gate)
2. `bun run typecheck` -- clean compilation
3. `node deprecation-stub/index.js` exits with code 1 and prints redirect message
4. `cat package.json | grep name` shows @chude/memory
5. `grep -rn "MemoryNexus\|MEMORY_NEXUS" src/ --include='*.ts'` -- zero results
6. MIGRATION.md, README.md, CLAUDE.md all reference new identity
7. Install command warns about stale hooks when old markers present
8. `~/.claude/rules/memory.md` exists with `memory` command references
9. `~/.claude/rules/memory-nexus.md` does NOT exist
10. `~/.claude/CLAUDE.md` references `rules/memory.md`
</verification>

<success_criteria>
- Deprecation stub exists as self-contained publishable package
- MIGRATION.md provides clear upgrade path for existing users
- README.md and CLAUDE.md fully updated to new identity
- External WoW rules file renamed and updated (memory-nexus.md -> memory.md)
- Global CLAUDE.md updated to reference new rules file
- Install command detects and warns about stale memory-nexus hooks
- Full test suite passes with no regressions
- No new TypeScript errors introduced by Phase 13 changes
- Phase 13 success criteria from ROADMAP.md all met:
  1. @chude/memory package with memory binary (package.json updated)
  2. XDG paths with auto-migration (paths.ts + migration.ts)
  3. Hooks reference memory binary (settings-manager updated, migration re-installs)
  4. Deprecation stub exists (deprecation-stub/ directory)
  5. All tests pass (bun test green)
</success_criteria>

<output>
After completion, create `.planning/phases/13-package-rename/13-03-SUMMARY.md`
</output>
