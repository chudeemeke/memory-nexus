---
phase: 28-friction-universalization
plan: 06
type: execute
wave: 1
depends_on: [28-04]
files_modified:
  - src/application/services/smart-context-service.test.ts
  - tests/integration/programmatic-api.test.ts
autonomous: true
requirements: [QUAL-01, QUAL-04]
gap_closure: true

must_haves:
  truths:
    - "SmartContextService daily logs test produces deterministic results regardless of wall clock time"
    - "Programmatic API doctor and sync tests complete within timeout on Windows"
  artifacts:
    - path: "src/application/services/smart-context-service.test.ts"
      constraint: "Daily log date filtering uses fixed reference dates, not Date.now()"
    - path: "tests/integration/programmatic-api.test.ts"
      constraint: "Doctor and sync tests have adequate timeout for Windows subprocess startup"
  key_links:
    - from: "smart-context-service.test.ts days filtering"
      to: "SmartContextService.getContext"
      via: "fixed dates in test data that are always within/outside the N-day window"
---

<objective>
Fix time-sensitive SmartContextService test and programmatic API subprocess timeouts.

Purpose: Close remaining 2 verification gaps: (1) daily logs filtered test uses real Date.now() making it time-sensitive near midnight and as dates in test data age, (2) executeDoctorCommand and executeSyncCommand tests timeout on Windows due to subprocess startup latency.
Output: Deterministic date-based test, adequate timeouts for integration tests.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/application/services/smart-context-service.test.ts
@src/application/services/smart-context-service.ts
@tests/integration/programmatic-api.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix time-sensitive daily logs test</name>
  <files>src/application/services/smart-context-service.test.ts</files>
  <action>
The "daily logs filtered to last N days when days option set" test at line 567 uses hardcoded dates (2026-03-09 as "recent", 2026-01-01 as "old") and `days: 7`. The SmartContextService.getContext() method computes the cutoff using `new Date()` (line 277 of smart-context-service.ts). As time passes, "2026-03-09" falls outside the 7-day window and the test fails.

The fix must inject a deterministic clock into SmartContextService, following existing DI patterns. Check the SmartContextService constructor deps interface to see if a `now` or `clock` dependency already exists. If not, add one.

**Approach A (preferred -- if constructor accepts deps):**
Add an optional `now?: () => Date` to the SmartContextService deps interface. Default to `() => new Date()`. Use `this.deps.now?.() ?? new Date()` at the callsite (line 277).

In the test, inject `now: () => new Date('2026-03-10T12:00:00Z')` so the 7-day window always includes 2026-03-09 and always excludes 2026-01-01.

**Approach B (if constructor does not accept deps easily):**
Change the test dates to be relative to a fixed reference point. Create test data with file paths that encode dates relative to a known anchor, and set `days` to a value that creates a clear boundary. For example:
- "recent" = daily file path `daily/2026-03-09.md`
- "old" = daily file path `daily/2020-01-01.md`
- Inject `now: () => new Date('2026-03-10T00:00:00Z')`

The key requirement: the test must NOT depend on the machine's wall clock. Either inject a clock or use dates so far apart that the test works for decades.

**Implementation steps:**
1. Read the SmartContextService constructor signature to understand the deps pattern
2. Add `now?: () => Date` to the deps interface
3. Use it in the cutoff calculation (line 277 area)
4. In the test, inject the fixed clock
5. Verify both the "filtered" and "default: no day limit" tests still pass

Do NOT use `jest.useFakeTimers()` or `bun` timer mocking -- use constructor DI for the clock. This is the standard pattern in this codebase (see BackfillService, ClaudeSummaryGenerator for DI examples).
  </action>
  <verify>
    <automated>cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/memory-nexus" && bun test src/application/services/smart-context-service.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Daily logs filtered test passes deterministically. The test uses an injected clock set to 2026-03-10, making 2026-03-09 always "recent" and 2026-01-01 always "old" regardless of when the test runs.</done>
</task>

<task type="auto">
  <name>Task 2: Fix programmatic API subprocess test timeouts</name>
  <files>tests/integration/programmatic-api.test.ts</files>
  <action>
The executeDoctorCommand and executeSyncCommand tests in the programmatic API integration test file have no explicit timeout. Bun's default test timeout is 5000ms. On Windows, these tests call real application code that initializes SQLite, loads extensions (FTS5, sqlite-vec), and performs file system operations -- all of which are slower on Windows than Linux.

**Investigation first:** Check if there are specific tests that timeout by running:
```bash
bun test tests/integration/programmatic-api.test.ts --timeout 30000 2>&1
```

If all tests pass with a higher timeout, the fix is simply adding appropriate timeouts.

**Fix:**
1. Add explicit timeouts to the slow tests. Bun test supports a second argument to `test()` for timeout:
```typescript
test("returns CommandResult with exitCode 0", async () => {
    // ...
}, 15000);
```

2. Add timeouts to these specific test groups/tests:
   - `executeSyncCommand` tests: 15000ms (sync initializes DB, scans files)
   - `executeDoctorCommand` tests: 15000ms (doctor checks multiple subsystems)
   - The "Return type validation" test that calls multiple commands: 30000ms

3. Do NOT add blanket timeouts to the entire describe block -- only to tests that need them. This keeps fast tests failing fast.

4. Check if the `beforeAll` or `afterAll` hooks also need timeout extensions.

5. If the tests still fail with 15s timeout, the issue may be that executeDoctorCommand or executeSyncCommand do real work (like downloading models or checking network). In that case, pass `{ quiet: true }` options to suppress output, and verify the commands have appropriate options to skip expensive checks.

Note: The "Return type validation" test at line 458 calls `executeSyncCommand`, `executeDoctorCommand`, and others sequentially -- it needs a cumulative timeout.
  </action>
  <verify>
    <automated>cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/memory-nexus" && bun test tests/integration/programmatic-api.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>All programmatic API tests pass on Windows without timeout failures. Slow tests have explicit timeouts. Fast tests remain at default timeout.</done>
</task>

</tasks>

<verification>
1. `bun test src/application/services/smart-context-service.test.ts` -- all tests pass, including daily logs filtering
2. `bun test tests/integration/programmatic-api.test.ts` -- all tests pass, no timeouts
3. Full suite: `bun test --no-coverage` -- no regressions
</verification>

<success_criteria>
- SmartContextService daily logs test uses injected clock, deterministic regardless of wall clock
- Programmatic API tests complete within their timeouts on Windows
- No test-only flags or environment detection hacks
</success_criteria>

<output>
After completion, create `.planning/phases/28-friction-universalization/28-06-SUMMARY.md`
</output>
