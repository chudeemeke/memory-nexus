---
phase: 29.1-test-determinism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/cli/commands/context.ts
  - src/presentation/cli/commands/search.ts
  - src/presentation/cli/commands/related.ts
  - src/presentation/cli/commands/context.test.ts
  - src/presentation/cli/commands/search.test.ts
  - src/presentation/cli/commands/related.test.ts
  - src/index.ts
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "Context command error handling tests complete in under 5 seconds each"
    - "Search command tests do not open the real user database"
    - "Related command tests do not open the real user database"
    - "All 3 execute functions accept dbPath via options for test isolation"
  artifacts:
    - path: "src/presentation/cli/commands/context.ts"
      provides: "executeContextCommand with dbPath injection"
      contains: "options.dbPath ?? getDefaultDbPath"
    - path: "src/presentation/cli/commands/search.ts"
      provides: "executeSearchCommand with dbPath injection"
      contains: "options.dbPath ?? getDefaultDbPath"
    - path: "src/presentation/cli/commands/related.ts"
      provides: "executeRelatedCommand with dbPath injection"
      contains: "options.dbPath ?? getDefaultDbPath"
  key_links:
    - from: "src/presentation/cli/commands/context.test.ts"
      to: "src/presentation/cli/commands/context.ts"
      via: "dbPath in options"
      pattern: "dbPath.*tempDir"
---

<objective>
Inject database path into context, search, and related execute functions via options-based DI, and update their tests to use temp databases.

Purpose: Eliminate 11 test failures caused by tests hitting the real ~266MB user database. Currently context error handling tests take 30-65 seconds each; they should complete in under 5 seconds.

Output: Three execute functions with dbPath injection, all tests using isolated temp DBs.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.1-test-determinism/29.1-RESEARCH.md

@src/presentation/cli/commands/context.ts
@src/presentation/cli/commands/search.ts
@src/presentation/cli/commands/related.ts
@src/presentation/cli/commands/context.test.ts
@src/presentation/cli/commands/search.test.ts
@src/presentation/cli/commands/related.test.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dbPath to execute function options and implementations</name>
  <files>
    src/presentation/cli/commands/context.ts
    src/presentation/cli/commands/search.ts
    src/presentation/cli/commands/related.ts
    src/index.ts
  </files>
  <action>
For each of context.ts, search.ts, and related.ts:

1. Add `dbPath?: string` to the command options interface (ContextCommandOptions, SearchCommandOptions, RelatedCommandOptions).
2. In the execute function, replace the hardcoded `getDefaultDbPath()` call with `options.dbPath ?? getDefaultDbPath()`. The pattern is: `const dbPath = options.dbPath ?? getDefaultDbPath();` then use `dbPath` where `getDefaultDbPath()` was previously called.
3. Ensure `getDefaultDbPath` import exists (it should already).

In src/index.ts (programmatic API), update the re-exported types to include the new dbPath field. Check if the programmatic API wrapper functions pass options through -- if they construct options internally, add dbPath passthrough.

Do NOT change any behavior for callers that omit dbPath -- they get `getDefaultDbPath()` as before. This is a backward-compatible, additive change.
  </action>
  <verify>
    <automated>cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/memory-nexus" && bunx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All three execute functions accept dbPath via options. TypeScript compiles with no errors. No behavioral change for production callers.</done>
</task>

<task type="auto">
  <name>Task 2: Update context, search, and related tests to use temp databases</name>
  <files>
    src/presentation/cli/commands/context.test.ts
    src/presentation/cli/commands/search.test.ts
    src/presentation/cli/commands/related.test.ts
  </files>
  <action>
For each test file that calls executeContextCommand, executeSearchCommand, or executeRelatedCommand:

1. Add beforeEach that creates a temp directory via `mkdtempSync(join(tmpdir(), "xxx-test-"))` and a dbPath within it.
2. Initialize the temp DB: `const { db } = initializeDatabase({ path: dbPath }); closeDatabase(db);` -- this creates schema without data.
3. Add afterEach that does `try { rmSync(tempDir, { recursive: true, force: true }); } catch {}`.
4. Pass `{ ...existingOptions, dbPath }` to every execute function call in tests.
5. Remove any `setTestDbPath` calls if present in these files.
6. Remove any explicit timeout overrides (e.g., `{ timeout: 15000 }`) that were added as workarounds for slow real-DB tests -- the temp DB makes them fast.

The tests verify error handling (nonexistent project returns exitCode 1, etc.) and formatting -- they don't need real session data. An empty schema DB is sufficient.

Import `mkdtempSync` from "fs", `tmpdir` from "os", `join` from "path", `initializeDatabase`/`closeDatabase` from the database module.
  </action>
  <verify>
    <automated>cd "C:/Users/Destiny/iCloudDrive/Documents/AI Tools/Anthropic Solution/Projects/memory-nexus" && bun test src/presentation/cli/commands/context.test.ts src/presentation/cli/commands/search.test.ts src/presentation/cli/commands/related.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All context, search, and related tests pass. Each test completes in under 5 seconds. No test opens the real user database.</done>
</task>

</tasks>

<verification>
1. `bun test src/presentation/cli/commands/context.test.ts` -- all pass, under 5s each
2. `bun test src/presentation/cli/commands/search.test.ts` -- all pass
3. `bun test src/presentation/cli/commands/related.test.ts` -- all pass
4. `bunx tsc --noEmit` -- no type errors
</verification>

<success_criteria>
- executeContextCommand, executeSearchCommand, executeRelatedCommand all accept optional dbPath
- All tests in the three test files pass using temp databases
- No test takes more than 5 seconds
- No test touches the real user database at ~/.local/share/memory/memory.db
</success_criteria>

<output>
After completion, create `.planning/phases/29.1-test-determinism/29.1-01-SUMMARY.md`
</output>
