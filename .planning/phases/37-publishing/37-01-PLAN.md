# Phase 37-01 Plan: Publishing

Status: Blocked by npm authentication
Created: 2026-05-30
Updated: 2026-05-30

## Publish Audit Findings

- `npm view @chude/memory version dist-tags --json` returned 404: the package is not visible on the public registry, or the current npm identity lacks access.
- `npm whoami` returned E401: the current shell is not authenticated to npm.
- RESOLVED in Phase 36.10: `package.json` repository metadata now points at `git+ssh://git@github.com/chudeemeke/memory-nexus.git`.
- RESOLVED in Phase 36.10: packed files include `README.md`, `package.json`, and `dist/**`; current dry-run package size is about 280.8 KB and unpacked size is about 1.0 MB.
- RESOLVED in Phase 36.10: publish build disables sourcemaps/declaration maps and cleans `dist` before rebuild; local absolute path scan passes.
- RESOLVED in Phase 36.10: README presents legacy `~/.memory` / `MEMORY_HOME` sidecars as explicit opt-in compatibility only.

## Goal

Publish `@chude/memory` only after local package contents, install behavior, registry auth, and release gates are verified.

## Tasks

1. Complete Phase 36.10. DONE.
2. Harden package metadata and build output. DONE.
   - Correct repository metadata.
   - Build with external packages/minification or an equivalent path-sanitizing configuration.
   - Verify packed output contains no local absolute paths or source maps.
3. Local install verification. DONE.
   - Build.
   - `npm pack --dry-run --json`.
   - Install the tarball into an isolated temp prefix.
   - Verify `memory --version`, `memory --help`, and a non-mutating health command.
4. Registry verification. BLOCKED.
   - Confirm npm authentication.
   - Confirm package availability and dist-tag strategy.
   - If first publish, use the scoped public access flag.
5. Release execution. BLOCKED.
   - Run final release gates.
   - Publish prerelease first if needed.
   - Verify install from registry before GA tagging.

## Current Blocker

Do not publish while npm auth is E401.

## Current Verification

- `npm whoami`: FAIL, E401 Unauthorized.
- `npm view @chude/memory version dist-tags --json`: FAIL, E404 not visible/not published or current identity lacks access.
- `npm pack --dry-run --json`: PASS; package contains `README.md`, `package.json`, and `dist/**`.
- Isolated tarball install smoke: PASS; verified `memory --version`, `memory --help`, and `memory status --json`.
- `npm publish --dry-run --access public`: PASS; dry-run reports `@chude/memory@4.0.0`, public access, package size about 280.8 KB, unpacked size about 1.0 MB, 184 files.

## Resume Instructions

1. Authenticate npm in the shell.
2. Re-run `npm whoami`.
3. Re-run `npm view @chude/memory version dist-tags --json`.
4. Re-run final gates from Phase 36.10 if any source/package file has changed.
5. Publish only after auth is valid and the package visibility/dist-tag choice is explicit.
