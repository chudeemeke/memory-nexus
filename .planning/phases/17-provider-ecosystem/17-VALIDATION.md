---
phase: 17
slug: provider-ecosystem
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-02-28
---

# Phase 17 — Validation Strategy

> Generated by `gsd-phase-researcher` during `/gsd:plan-phase 17`.
> Updated by `gsd-plan-checker` after plan approval.
> Governs feedback sampling during `/gsd:execute-phase 17`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3.x |
| **Config file** | vitest.config.ts |
| **Quick run command** | `bun test --bail` |
| **Full suite command** | `bun test` |
| **Estimated runtime** | ~15 seconds |
| **CI pipeline** | .github/workflows/release.yml -- exists |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `bun test --bail`
- **After every plan wave:** Run `bun test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | PROV-01 | unit | `bun test tests/unit/infrastructure/embedding/ --bail` | TBD | pending |
| TBD | TBD | TBD | PROV-02 | unit | `bun test tests/unit/infrastructure/embedding/ --bail` | TBD | pending |
| TBD | TBD | TBD | PROV-03 | integration | `bun test tests/integration/ --bail` | TBD | pending |
| TBD | TBD | TBD | PROV-04 | integration | `bun test tests/integration/ --bail` | TBD | pending |

*Status values: pending / green / red / flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

- [ ] `tests/unit/infrastructure/embedding/openai-provider.test.ts` -- stubs for PROV-01
- [ ] `tests/unit/infrastructure/embedding/ollama-provider.test.ts` -- stubs for PROV-02
- [ ] `tests/integration/provider-switch.test.ts` -- stubs for PROV-03, PROV-04

*Existing test infrastructure covers framework and fixtures -- no Wave 0 framework setup needed.*

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.
> These are surfaced during `/gsd:verify-work` UAT.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| OpenAI API key validation against live endpoint | PROV-01 | Requires valid API key and network access | Set `provider: "openai"` with valid key, run `memory sync --embed`, verify embeddings generated |
| Ollama server connectivity | PROV-02 | Requires running Ollama server | Start Ollama, set `provider: "ollama"`, run `memory sync --embed`, verify embeddings generated |

---

## Validation Sign-Off

Updated by `gsd-plan-checker` when plans are approved:

- [ ] All tasks have automated verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/gsd:execute-phase 17`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 0 | TBD | -- | -- | -- | scaffold |
| 1 | TBD | TBD | TBD | TBD | pending |
| 2 | TBD | TBD | TBD | TBD | pending |

**Phase validation complete:** pending
