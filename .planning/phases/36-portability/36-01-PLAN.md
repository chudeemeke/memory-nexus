---
phase: 36-portability
plan: 01
type: execute
wave: 1
depends_on: [35]
files_modified:
  - src/presentation/cli/commands/migrate.ts
  - src/presentation/cli/commands/doctor.ts
  - src/presentation/cli/commands/index.ts
  - src/presentation/cli/index.ts
  - src/presentation/cli/commands/migrate.test.ts
  - src/presentation/cli/commands/doctor.portability.test.ts
autonomous: true
requirements: [PORT-01, PORT-02, PORT-03]
must_haves:
  truths:
    - "memory migrate --from-windows executes WAL checkpointing and integrity check successfully"
    - "memory migrate --from-windows re-installs Git hooks for the current platform context"
    - "memory migrate --from-windows prints a project-grouped sessions summary"
    - "memory doctor --portability detects and reports mixed-environment path slashes and absolute drives"
    - "memory doctor --portability detects and lists database session paths that do not exist on the current filesystem"
    - "memory doctor --portability checks and verifies sqlite-vec availability"
    - "All unit/integration tests for migrate and doctor --portability commands pass green"
---

<objective>
Implement platform portability and WSL transition support inside memory-nexus via a new `memory migrate --from-windows` command and an expanded `memory doctor --portability` diagnostic mode, ensuring users can safely transfer database session data and recover hook integration across Windows/Linux environments.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement memory migrate --from-windows command</name>
  <files>src/presentation/cli/commands/migrate.ts, src/presentation/cli/commands/migrate.test.ts</files>
  <behavior>
    - Test: command executes SQLite wal_checkpoint(TRUNCATE) successfully
    - Test: command executes integrity_check successfully and fails if DB corrupted
    - Test: command calls uninstallHooks and installHooks to re-establish Git hooks
    - Test: command outputs project-grouped statistics session count summary
  </behavior>
  <action>
    - Create `src/presentation/cli/commands/migrate.ts` exportable command class/functions.
    - Inside `executeMigrateCommand`, initialize the SQLite database connection.
    - Perform PRAGMA checkpoints:
      - `db.run("PRAGMA wal_checkpoint(TRUNCATE)")` to flush and commit WAL logs.
    - Perform database health scans:
      - Query `PRAGMA integrity_check` and confirm the value equals `ok`. If it fails, throw a custom MemoryError or report corruption.
    - Re-install Git hooks:
      - Call `uninstallHooks()` then `installHooks()` from hooks services to clean up old platform references.
    - Fetch project session summaries:
      - Use `SqliteStatsService` to fetch stats and output a beautiful formatted summary grouped by project showing sessions and messages counts.
    - Write comprehensive test coverage in `migrate.test.ts` mocking SQLite operations and hooks helpers.
  </action>
  <verify>
    <automated>bun test src/presentation/cli/commands/migrate.test.ts</automated>
  </verify>
  <done>migrate command implemented; checkpointing, integrity validation, and hook re-installation wired; full unit test suite passing green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand memory doctor --portability option</name>
  <files>src/presentation/cli/commands/doctor.ts, src/presentation/cli/commands/doctor.portability.test.ts</files>
  <behavior>
    - Test: doctor --portability flag registers cleanly
    - Test: doctor --portability analyzes path dialects and reports Windows backslashes / absolute letters
    - Test: doctor --portability lists database records pointing to non-existent filesystem locations
    - Test: doctor --portability outputs clean reports with appropriate color coding
  </behavior>
  <action>
    - Update `DoctorOptions` in `doctor.ts` to include `portability?: boolean`.
    - Update `createDoctorCommand()` to support `--portability` option.
    - Inside `executeDoctorCommand()`, if `options.portability` is active, perform dedicated portability scans:
      - Open database connection.
      - Check path formats: Query `SELECT project_path_decoded, project_name FROM sessions`.
        - Count paths starting with a drive letter (e.g. `C:`) or containing `\`.
        - Count paths starting with `/` or containing `/`.
        - If both are present, report a mixed-environment warning.
      - Check orphaned locations: For each unique session project path, check if `fs.existsSync(path)` is false.
        - List any projects that are cataloged in the database but do not exist on the host filesystem.
      - Check vector extension: Query status helper to report `sqlite-vec` engine availability.
    - Print the portability report using clean colors and formatted status logs.
    - Write robust unit tests inside `doctor.portability.test.ts` to assert all path analysis and orphaned folder outputs.
  </action>
  <verify>
    <automated>bun test src/presentation/cli/commands/doctor.portability.test.ts</automated>
  </verify>
  <done>doctor --portability option registered; mixed path styles and orphaned records detected; full unit coverage verified green</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Export and Register commands</name>
  <files>src/presentation/cli/commands/index.ts, src/presentation/cli/index.ts</files>
  <action>
    - Export `createMigrateCommand` and `executeMigrateCommand` inside `src/presentation/cli/commands/index.ts`.
    - Import and register the new command in `src/presentation/cli/index.ts` under the "System Commands" or "Data Commands" group using `program.addCommand(createMigrateCommand())`.
  </action>
  <verify>
    <automated>bun test src/presentation/cli/commands/index.ts</automated>
  </verify>
  <done>Migrate command fully registered in CLI program</done>
</task>

</tasks>

<verification>
- `bun test src/presentation/cli/commands/migrate.test.ts` -- all migration command tests pass
- `bun test src/presentation/cli/commands/doctor.portability.test.ts` -- all portability diagnostic tests pass
- `bun run test:isolation` -- 0 test isolation violations
- `bun test` -- complete Vitest suite passes
</verification>

<success_criteria>
1. PORT-01: `memory migrate --from-windows` executes SQLite WAL checkpointing and integrity verification successfully before platform transition.
2. PORT-02: `memory migrate --from-windows` reinstalls local Git hooks with the correct path dialects for the target platform.
3. PORT-03: `memory doctor --portability` accurately reports mixed path formatting dialects (backslashes vs slashes) and lists cataloged directories that do not exist on the current filesystem.
4. All 3,600+ Vitest cases continue passing with no regressions.
</success_criteria>
