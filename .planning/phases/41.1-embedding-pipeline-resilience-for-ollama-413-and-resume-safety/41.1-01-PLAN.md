---
phase: 41.1-embedding-pipeline-resilience-for-ollama-413-and-resume-safety
plan: 01
type: execute
wave: 1
depends_on: []
requirements_addressed:
  - EMBED-RES-01
  - EMBED-RES-02
  - EMBED-RES-03
  - EMBED-RES-04
  - EMBED-RES-05
files_modified:
  - src/domain/ports/embedding.ts
  - src/domain/ports/repositories.ts
  - src/application/services/embedding-service.ts
  - src/infrastructure/embedding/ollama-provider.ts
  - src/infrastructure/database/repositories/embedding-repository.ts
  - src/infrastructure/database/schema.ts
  - src/infrastructure/hooks/config-manager.ts
  - src/presentation/cli/commands/sync/embedding-pass.ts
  - src/**/*.test.ts
autonomous: true

must_haves:
  truths:
    - "Embedding resume is monotonic: deterministic single-item payload failures do not block later rows."
    - "Provider request limits are handled without hard-coding Kanbanflow, Tailscale, or a single endpoint."
    - "Ollama HTTP 413 receives concrete coverage and maps to typed provider failure."
    - "Durable skip/quarantine records are model-scoped and privacy-safe."
    - "CLI output reports skipped rows honestly without raw transcript content."
  artifacts:
    - path: "src/domain/ports/embedding.ts"
      provides: "Typed embedding provider payload-too-large failure contract"
    - path: "src/application/services/embedding-service.ts"
      provides: "Byte-bounded batching, durable skip handling, monotonic resume"
    - path: "src/infrastructure/database/repositories/embedding-repository.ts"
      provides: "SQLite skip/quarantine persistence and current-model filtering"
    - path: "src/infrastructure/embedding/ollama-provider.ts"
      provides: "Ollama 413 adaptive split and typed single-item failure"
    - path: "src/presentation/cli/commands/sync/embedding-pass.ts"
      provides: "Safe skipped-count reporting"
  key_links:
    - from: "src/application/services/embedding-service.ts"
      to: "src/domain/ports/embedding.ts"
      via: "typed payload-too-large error import"
      pattern: "Embedding.*Payload.*Too.*Large|payload_too_large"
    - from: "src/infrastructure/database/repositories/embedding-repository.ts"
      to: "src/infrastructure/database/schema.ts"
      via: "embedding skip/quarantine table"
      pattern: "embedding_.*(skip|failure|quarantine)"
---

<objective>
Implement Phase 41.1 embedding pipeline resilience so oversized embedding requests cannot wedge `memory sync --embed`.

Output: typed provider failure handling, provider/transport byte-bounded batching, durable model-scoped skip/quarantine state, resume filtering, safe CLI reporting, and tests proving the behavior.
</objective>

<execution_context>
@C:\Users\Destiny\.codex\get-shit-done\workflows\execute-plan.md
@C:\Users\Destiny\.codex\get-shit-done\templates\summary.md
@C:\Users\Destiny\.codex\get-shit-done\references\tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-embedding-pipeline-resilience-for-ollama-413-and-resume-safety/41.1-CONTEXT.md
@.planning/phases/41.1-embedding-pipeline-resilience-for-ollama-413-and-resume-safety/41.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED provider tests for Ollama payload-too-large behavior</name>
  <read_first>
src/domain/ports/embedding.ts
src/infrastructure/embedding/ollama-provider.ts
src/infrastructure/embedding/ollama-provider.test.ts
  </read_first>
  <action>
Add failing tests that prove:
- `OllamaEmbeddingProvider.embedBatch(["a", "b", "c", "d"])` retries by splitting when the first `/api/embed` call returns HTTP 413.
- Split retries preserve output order.
- `OllamaEmbeddingProvider.embedBatch([singleOversizedText])` throws a typed payload-too-large error with `kind` or `code` equal to `payload_too_large`, `status` equal to `413`, and no raw text in the error message or metadata.
  </action>
  <acceptance_criteria>
- `bun test src/infrastructure/embedding/ollama-provider.test.ts --timeout 15000` fails before implementation on the new assertions.
- The new test file contains the string `payload_too_large`.
- The new tests assert that raw oversized text is not present in the thrown error message.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add RED application and repository tests for monotonic resume</name>
  <read_first>
src/application/services/embedding-service.ts
src/application/services/embedding-service.test.ts
src/domain/ports/repositories.ts
src/infrastructure/database/repositories/embedding-repository.ts
tests/infrastructure/database/repositories/embedding-repository.test.ts
src/infrastructure/database/schema.ts
src/infrastructure/database/schema.test.ts
  </read_first>
  <action>
Add failing tests that prove:
- `EmbeddingService` honors a `maxBatchBytes` config value and sends smaller sub-batches even when `batchSize` is higher.
- When a provider throws typed `payload_too_large` for one item, the service marks that message skipped/quarantined and continues with later messages.
- `IEmbeddingRepository.findUnembedded(limit, modelHash)` excludes skip/quarantine rows for the same model hash.
- A skip/quarantine for an old model hash does not hide the same message from a new model hash.
  </action>
  <acceptance_criteria>
- Focused application/repository tests fail before implementation.
- Repository tests assert the same message can be hidden for one model hash and visible for another.
- Test names include `monotonic resume` or `model-scoped skip`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement provider, config, schema, and service behavior</name>
  <read_first>
src/domain/ports/embedding.ts
src/domain/ports/repositories.ts
src/application/services/embedding-service.ts
src/infrastructure/embedding/ollama-provider.ts
src/infrastructure/database/repositories/embedding-repository.ts
src/infrastructure/database/schema.ts
src/infrastructure/hooks/config-manager.ts
  </read_first>
  <action>
Implement the behavior required by Tasks 1 and 2:
- Add a typed embedding provider error for `payload_too_large`.
- Map Ollama HTTP 413 to the typed error.
- For multi-item Ollama batches, split recursively or iteratively and concatenate results in original input order.
- Add `maxBatchBytes` to embedding config with a conservative default and validation.
- Add durable model-scoped skip/quarantine persistence with privacy-safe metadata only.
- Update `findUnembedded` and embedding service calls so current-model skipped rows are excluded from resume.
- Continue embedding later rows after a handled single-item skip.
  </action>
  <acceptance_criteria>
- `rg "payload_too_large" src` finds domain/provider/service usage.
- `rg "maxBatchBytes" src` finds config and service usage.
- `rg "embedding_.*(skip|failure|quarantine)" src/infrastructure/database/schema.ts` finds the durable table.
- Focused provider, service, repository, and schema tests pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update CLI reporting and cross-project coordination</name>
  <read_first>
src/presentation/cli/commands/sync/embedding-pass.ts
src/presentation/cli/commands/sync/embedding-pass.test.ts
docs/inbox/2026-06-22-kanbanflow-embed-413-oversized-batch-stalls-reembed.md
docs/inbox/2026-06-17-tailscale-ollama-egress-endpoint-confirmed.md
  </read_first>
  <action>
Update sync embedding output so handled skips are visible and safe:
- Human output includes an embedded count and skipped count when skipped is non-zero.
- JSON output includes `skipped`.
- Generic provider failures still fail loudly, but payload-too-large single-item skips are reported as handled skip state.
- Add or update Kanbanflow counter-notification in `C:\Projects\kanbanflow\docs\inbox\` after the phase is implemented or in progress.
  </action>
  <acceptance_criteria>
- CLI tests assert skipped count appears without raw message text.
- `node C:\Projects\conversations\scripts\inbox-lint.cjs --all --quiet` passes for memory-nexus inbox.
- The Kanbanflow counter-notification passes `node C:\Projects\conversations\scripts\inbox-lint.cjs <file>`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Verify and close the GSD phase</name>
  <read_first>
package.json
.planning/phases/41.1-embedding-pipeline-resilience-for-ollama-413-and-resume-safety/README.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/STATE.md
  </read_first>
  <action>
Run focused tests first, then broader gates appropriate to this risk:
- Provider/service/repository/schema/CLI focused tests.
- `bun run typecheck`.
- `bun run build`.
- `bun run eval:v5`.
- Inbox lint.
- `git diff --check`.
If focused implementation touches shared schema or service contracts, also run `bun test --timeout 15000` or `bun run quality` if time and machine resources allow.
Update README, ROADMAP, REQUIREMENTS, STATE, and create `41.1-01-SUMMARY.md` with exact verification results.
  </action>
  <acceptance_criteria>
- Verification commands and outcomes are recorded in `41.1-01-SUMMARY.md`.
- ROADMAP marks Phase 41.1 plan complete only if implementation and verification actually passed.
- REQUIREMENTS marks EMBED-RES-01 through EMBED-RES-05 complete only if tests prove them.
- No claim says installed `memory` is fixed unless an installed/local binary smoke was actually run.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Ollama 413 multi-item adaptive split preserves order.
- [ ] Ollama 413 single-item failure is typed and privacy-safe.
- [ ] Application batching honors `maxBatchBytes`.
- [ ] Single oversized item is durably skipped/quarantined and later rows continue.
- [ ] Resume excludes current-model skipped rows and allows new-model retry.
- [ ] CLI output reports skipped count safely.
- [ ] Focused tests, typecheck, build, inbox lint, and diff whitespace pass.
</verification>

<success_criteria>
1. `memory sync --embed` can make progress after deterministic oversized single-item failures.
2. No raw transcript content is stored or printed in skip/quarantine diagnostics.
3. Provider-specific Ollama 413 handling is implemented without hard-coding consumers or endpoints.
4. The fix is covered by tests at provider, application, repository, schema, and CLI levels.
5. Cross-project Kanbanflow blocker is counter-notified with the implementation status.
</success_criteria>

<output>
After completion, create `.planning/phases/41.1-embedding-pipeline-resilience-for-ollama-413-and-resume-safety/41.1-01-SUMMARY.md`.
</output>
