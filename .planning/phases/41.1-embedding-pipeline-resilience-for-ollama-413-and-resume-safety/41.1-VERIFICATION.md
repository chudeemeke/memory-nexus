---
status: passed
phase: 41.1-embedding-pipeline-resilience-for-ollama-413-and-resume-safety
verified_at: 2026-06-22T03:07:50+01:00
requirements:
  - EMBED-RES-01
  - EMBED-RES-02
  - EMBED-RES-03
  - EMBED-RES-04
  - EMBED-RES-05
market_ready: false
market_ready_reason: "Phase 41.1 source gates pass, but eval:v5 still reports 2 contract fixtures requiring behavior-backed implementation in later phases."
---

# Phase 41.1 Verification

## Result

Passed for Phase 41.1 source implementation.

Not final market-ready approval. `bun run eval:v5` still reports `market_readiness.eligible=false` because two contract fixtures remain owned by Phase 42/43 readiness work.

## Requirement Evidence

- **EMBED-RES-01:** `EmbeddingService` honors configured `maxBatchBytes` and count-based `batchSize`; config validation covers `maxBatchBytes`.
- **EMBED-RES-02:** `OllamaEmbeddingProvider` retries multi-item 413 responses through split batches and preserves result ordering.
- **EMBED-RES-03:** `embedding_skips` plus repository APIs persist model-scoped single-item skip/quarantine records with safe metadata.
- **EMBED-RES-04:** Resume filtering excludes current-model skips, permits new-model retry, continues later rows, and reports embedded/skipped counts.
- **EMBED-RES-05:** Provider, application, repository, schema, config, CLI, and domain error tests cover the behavior without raw transcript output.

## Gates

- `bun test src/presentation/cli/commands/dream.test.ts --timeout 15000`: PASS, 10 pass.
- `bun test src/domain/ports/embedding.test.ts --timeout 15000`: PASS, 18 pass.
- `bun run typecheck`: PASS.
- `bun run build`: PASS.
- `bun run test:isolation`: PASS.
- `bun run eval:v5`: PASS, 9/9 fixtures; market readiness not eligible because 2 contract fixtures remain.
- `bun run test:coverage`: PASS, 4364 pass, 0 fail; statements 97.35%, branches 95.00%, functions 96.59%, lines 97.45%.
- `bun test --timeout 15000`: PASS, 4364 pass, 0 fail.
- `bun audit`: PASS, no vulnerabilities.
- `git diff --check`: PASS.
- `node C:\Projects\conversations\scripts\inbox-lint.cjs --all --quiet`: PASS.

## Release Notes

- The checked source is fixed and gated.
- Local install is fixed: the global Bun `memory` command now resolves to `C:\Projects\memory-nexus-hotfix-41-1` at commit `03cbe28`, built from this phase without Phase 42/Dreaming WIP.
- Installed runtime checks passed: `memory status --json` reported Ollama provider egress allowed for `ollama.tail859c3a.ts.net` and `maxBatchBytes: 800000`; live embedding progressed from about `176100` to `180100` embedded rows before the long foreground run was stopped deliberately.
- Installed public API smoke passed for byte-bounded batching under a `512` byte cap.
- Focused clean-worktree tests passed for `EmbeddingService`, `EmbeddingRepository`, and sync embedding pass, including model-scoped `payload_too_large` skip and resume behavior.
- npm registry is not fixed yet. `@chude/memory@4.0.0` is already published; registry-level distribution requires a `4.0.1` patch publish with OTP.
- GSD update was attempted but still fails in the upstream Codex installer after writing `VERSION 1.32.0`; the issue remains tracked in the get-stuff-done inbox.
