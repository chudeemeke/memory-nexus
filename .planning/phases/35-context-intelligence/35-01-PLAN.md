---
phase: 35-context-intelligence
plan: 01
type: execute
wave: 1
depends_on: [33, 34]
files_modified:
  - src/application/services/smart-context-service.ts
  - src/application/services/smart-context-service.test.ts
  - src/presentation/cli/commands/context.ts
  - src/presentation/cli/commands/context.test.ts
  - src/presentation/cli/db-startup.ts
  - src/infrastructure/database/health-checker.ts
autonomous: true
requirements: [CTXT-01, CTXT-02, CTXT-03, CTXT-04]
must_haves:
  truths:
    - "SmartContextService queries the facts projection table directly instead of legacy ~/.memory/ files"
    - "SmartContextService builds ordered sections: Active Decisions, Recent Learnings, User Preferences, Open Friction, and Session Summary fallback"
    - "SmartContextService supports --global flag which aggregates preferences, learnings, and decisions across all projects"
    - "CLI command memory context returns the new facts-based briefing"
    - "CLI command memory context outputs deprecation warning if ~/.memory/ folder exists"
    - "CLI command memory context supports --json output formatted under QueryResultEnvelope"
    - "All unit/integration tests for SmartContextService and context CLI command pass"
  artifacts:
    - path: "src/application/services/smart-context-service.ts"
      provides: "SmartContextService rewired to facts and friction SQLite projection database tables"
    - path: "src/presentation/cli/commands/context.ts"
      provides: "Context command supporting new structure, --global, --json, and legacy deprecation warning"
---

<objective>
Rewire the SmartContextService to fetch facts directly from the SQLite facts projection database (active decisions, learnings, preferences, observations) and open friction log entries, fully deprecate the legacy ~/.memory/ directory convention, and support robust cross-project context querying (--global).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-context-intelligence/35-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire SmartContextService to use SQLite facts and friction tables</name>
  <files>src/application/services/smart-context-service.ts, src/application/services/smart-context-service.test.ts</files>
  <read_first>
    - src/application/services/smart-context-service.ts (current implementation using memoryFileRepo)
    - src/domain/ports/repositories.ts (IFactRepository, IFrictionRepository contracts)
  </read_first>
  <behavior>
    - Test: SmartContextService retrieves and parses active 'decision' facts for a project as 'Active Decisions' section
    - Test: SmartContextService retrieves and parses active 'learning' facts for a project as 'Recent Learnings' section
    - Test: SmartContextService retrieves and parses active 'preference' facts for a project as 'User Preferences' section
    - Test: SmartContextService retrieves and parses active 'observation' facts for a project as 'Observations' section
    - Test: SmartContextService retrieves and parses open frictions from frictionRepo and formats them as 'Open Friction'
    - Test: SmartContextService retrieves global facts when crossProject option is true
    - Test: SmartContextService applies precision token budgeting using allocateBudget
  </behavior>
  <action>
    - Replace `memoryFileRepo: IMemoryFileRepository` with `factRepo: IFactRepository` in `SmartContextDeps` interface and constructor.
    - Inside `getContext(options)`, query `this.factRepo.findByProject(projectName)`.
    - Filter facts to exclude superseded ones: `const activeFacts = allFacts.filter(f => f.supersededAt === null)`.
    - Group active facts by `type`:
      - Decisions: `activeFacts.filter(f => f.type === "decision")`
      - Learnings: `activeFacts.filter(f => f.type === "learning")`
      - Preferences: `activeFacts.filter(f => f.type === "preference")`
      - Observations: `activeFacts.filter(f => f.type === "observation")`
    - Formulate context sections using standard markdown checklists (`- [ ] UUID: content` or similar).
    - If `options.crossProject` is true:
      - Query facts across other projects (or all projects): `const allFactsGlobal = await this.factRepo.findAll()` or filter appropriately.
      - Add sections for "Global/Cross-Project User Preferences", "Cross-Project Decisions", "Cross-Project Learnings".
    - Update `smart-context-service.test.ts` to mock `IFactRepository` instead of `IMemoryFileRepository`, updating assertions to match the new facts-based context sections.
  </action>
  <verify>
    <automated>bun test src/application/services/smart-context-service.test.ts</automated>
  </verify>
  <done>SmartContextService successfully rewired to facts table; active decisions, learnings, and preferences grouped; global context queries supported; full unit test suite passing green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update CLI context command and handle legacy deprecation warning</name>
  <files>src/presentation/cli/commands/context.ts, src/presentation/cli/commands/context.test.ts, src/presentation/cli/db-startup.ts</files>
  <read_first>
    - src/presentation/cli/commands/context.ts (current CLI command)
    - src/presentation/cli/db-startup.ts (XDG path checks and db initialization)
  </read_first>
  <behavior>
    - Test: memory context outputs new facts briefing in standard text mode
    - Test: memory context outputs JSON envelope matching QueryResultEnvelope with --json option
    - Test: memory context prints deprecation warning if ~/.memory/ folder exists on filesystem
    - Test: context command supports --global flag and passes it to service
  </behavior>
  <action>
    - Update `src/presentation/cli/commands/context.ts` dynamic dependencies imports. Import `SqliteFactRepository` and inject it into `SmartContextService` constructor instead of `SqliteMemoryFileRepository`.
    - Update `executeContextCommand` to check for legacy `~/.memory/` folder on disk:
      - Get legacy directory: `join(os.homedir(), ".memory")`.
      - If `fs.existsSync(legacyDir)` is true and not quiet, output warning to stderr:
        `[DEPRECATION WARNING] Legacy memory directory ~/.memory/ is deprecated. Your knowledge and decisions are now stored safely in the SQLite database.`
    - Bind `--json` to `QueryResultEnvelope` formatting wrapper so that `memory context --json` returns standard JSON envelopes.
    - Wire the `--global` flag as an alias/option to `crossProject` parameter.
    - Update `context.test.ts` to assert that the deprecation warning is printed when `~/.memory` folder exists, and that standard query output is cleanly returned.
  </action>
  <verify>
    <automated>bun test src/presentation/cli/commands/context.test.ts</automated>
  </verify>
  <done>CLI context command rewired; deprecation warning for ~/.memory/ implemented; --json envelope supported; full unit tests validated</done>
</task>

</tasks>

<verification>
- `bun test src/application/services/smart-context-service.test.ts` -- all service tests pass
- `bun test src/presentation/cli/commands/context.test.ts` -- all command tests pass
- `bun run test:isolation` -- 0 isolation violations
- `bun test` -- complete Vitest suite passes (3,600+ tests green)
</verification>

<success_criteria>
1. CTXT-01: memory context default output is an AI briefing built from active decisions, learnings, preferences, and open frictions in SQLite facts.
2. CTXT-02: --global flag aggregates active user preferences, learnings, and decisions across all projects.
3. CTXT-03: No context queries or sync operations read from or write to the legacy ~/.memory/ directory.
4. CTXT-04: Active deprecation warning printed if ~/.memory/ exists.
5. All 3,600+ tests in memory-nexus suite continue to pass cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/35-context-intelligence/35-01-SUMMARY.md` and updateSTATE.md / ROADMAP.md.
</output>
