# Phase 36.9-01 Summary: Coverage Runner Migration and Remediation

Status: Complete
Completed: 2026-05-30

## Outcome

Phase 36.9 restored the honest four-metric release gate. The release coverage command now measures and enforces statements, branches, functions, and lines independently instead of relying on unavailable Bun LCOV metrics.

Final `bun run test:coverage` result:

- Tests: 4,050 passed, 0 failed
- Statements: 97.18% (7103/7309)
- Branches: 95.00% (4049/4262)
- Functions: 96.09% (1303/1356)
- Lines: 97.32% (6805/6992)

## Implementation

- Kept the Istanbul-backed Bun coverage harness as the release gate.
- Added focused behavioral tests for sync events, hook-runner summaries, sync-helper dry runs and reports, doctor defaults, status branches, export JSON and default error paths, install stale-hook scanning, settings uninstall behavior, backfill TTY progress, list invalid date handling, progress reporter TTY/download branches, and redactor circular JSON handling.
- Removed unreachable defensive branches in `ContentExtractor` after the message-type guard made those branches non-executable.
- Simplified environment-secret redaction after the regex shape guaranteed the assignment prefix.

## Verification

- `bun run test:coverage`: PASS
- `bun run typecheck`: PASS
- `bun run build`: PASS
- `bun run test:isolation`: PASS
- `bun audit`: PASS, no vulnerabilities
- `gitleaks detect --no-banner --redact --source .`: PASS, no leaks

## Closure Commits

- `b88276a test: cover sync event and status branches`
- `d91d851 test: close coverage gate branches`

## Notes

- Phase 37 can start because the coverage and foundation gates are no longer the blocker.
- Branch coverage is exactly 95.00%, so Phase 37 must preserve test-first discipline for any publish-path code changes.
- Remaining misses are not hidden: they are integration/error branches outside the current release blocker, and any touched surface must bring its own focused tests.
