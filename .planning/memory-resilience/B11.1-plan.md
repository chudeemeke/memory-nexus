# B11.1 - Owned database fixture cleanup

Status: active. Starting source: 61c757543638b6249dfaa6fed7e5ccaecd575511.

The helper currently marks cleanup complete before close/removal, discards both
failures and deletes whatever occupies its temporary path. This repair is test
infrastructure only. Existing/replaced directories must survive cleanup; failed
cleanup must remain diagnosable and retryable. Reboot reclamation is not assumed.

1. Reproduce replacement deletion through createTestDir using a newly allocated
   synthetic fixture; preserve recovery handles and explicitly reclaim only
   verified probe-owned paths.
2. Introduce one reusable owned temporary-directory primitive for both helpers.
   Bind exclusive allocation to canonical parent/path, directory identity and an
   exclusive ownership marker. Verify before deletion. Preserve ownership after
   partial removal only when the original directory identity still matches.
3. Make database cleanup close once after successful closure, retry failed close
   or removal, and reclaim initialization failures without hiding the primary
   error. Existing callers keep the same create/cleanup interface; cleanup failure
   now throws a path-bearing error instead of silently succeeding.
4. Exercise real SQLite success, repeated cleanup, replacement/link/marker
   refusal, close/init/removal failure and partial cleanup recovery. Use local
   dependency injection for failure boundaries, not process-wide module mocks.
5. Refresh changed/new inventory entries as proposed and invalidate their older
   metric evidence. Run focused caller checks and targeted type checking; record
   remaining platform, complete Tier S metrics and independent-review obligations.

B04 remains awaiting independent review and is not verified. B11.1 can proceed
because its prerequisite B03 has scoped proof. Neither this plan nor a green
focused test run closes the final B11.1 quality/review gate by itself.

## Platform checkpoint

Windows external-lock recovery, marker allocation/restoration failure and Linux
caller tests are now retained in evidence/B11.1-platform.json. Real EBUSY repeats
required bounded 100/200 ms recollection retries; failures remain diagnostic and
retryable after exhaustion. Final helper diagnostic metrics reach 100%, and one
inode mutation is killed. Driver coverage/applicability, broader decision checks
and independent review remain open; B11.1 is still active.

## Runtime correction and current restart

The earlier paced-retry checkpoint is historical: EBUSY later recurred.
`evidence/B11.1-boundaries-runtime.json` retains actual Bun lock-owner evidence,
paired teardown experiments and the event-loop-yield mitigation. Current Windows
and Linux focused checks pass with the existing quality invocation timeout.
Driver branches remain 58/70; complete those failure/recovery decisions and
platform applicability before acceptance. Default timeout behavior also remains
owned by B11.1. Eight killed mutations and green focused checks do not close
the independent review, instrumenter or full baseline gates.

## Timeout invocation repair

Synthetic 10 ms TOML/CLI comparisons show the installed Windows Bun 1.4.1 and
Linux Bun 1.3.14 both load the configured preload but ignore the TOML timeout.
Before editing the package script, reproduce a 5.25-second synthetic test failure
through `bun run test`. Then give that entrypoint the existing quality command's
explicit 15000 ms limit and verify the same test on both platforms. Document that
direct `bun test` needs the flag; do not imply TOML enforces it. This disposition
does not relax the existing quality invocation or accept default-run failures.

Timeout repair is verified through the package entrypoint on Windows and Linux.
See `evidence/B11.1-timeout-invocation.json`. Combined same-source driver counters
now identify nine remaining failure/recovery outcomes (61/70 branches). Continue
with lock-holder startup and interrupted teardown, then adversarial-fixture
recovery. The full quality and independent-review gates remain open.

## Lock-holder failure lifecycle

Extract the existing child lifecycle within the regression driver so success,
startup rejection, missing executable and interrupted callback paths exercise the
same code. Use real child processes, owned script/target fixtures and a bounded
child watchdog. Await child termination before fixture cleanup, retain the primary
callback failure, and prove exactly one explicit termination when needed. These
are characterization and recovery tests; do not manufacture a production bug.
Re-measure unchanged-map platform coverage and kill an omitted-teardown mutation
before claiming this new decision path is verified.

Local lifecycle and driver decision proof is retained in
`evidence/B11.1-holder-proof.json`. B11.1 is now blocked on final Linux grouped
execution (WSL CreateVm failure), independent review and final measurement
acceptance. Continue authorized B11.2 work without marking these gates complete.
