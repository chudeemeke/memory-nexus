# B11.4 - Owned JSON-command test storage

Status: active after B11.3 retirement. Owner: memory-nexus.

The current helper returns a random database path without creating an exclusively
owned container. Cleanup accepts arbitrary tracker strings, deletes DB/WAL/SHM
paths, swallows errors and clears the tracker even when removal fails.

Consumers verified by source search: search, context, show, list, related and stats
`.json.test.ts` files, plus `_helpers/capture-json.test.ts`. The similarly named
local functions in `list.deps.test.ts` and `stats.deps.test.ts` are not imports of
this helper. Read the helper's existing tests and all seven callers before edits.

1. Reproduce foreign/replaced path refusal and failure-retention requirements in
   owned synthetic fixtures. A tracker string alone must never authorize deletion.
2. Reuse the existing owned-directory capability. Preserve the caller's ability to
   request a nonexistent DB path, because missing-database cases depend on that.
   Keep authority until successful cleanup; failures retain a useful path and
   error and can be retried. Verify partial failures and multiple tracked fixtures.
3. Resolve the build boundary before introducing shared test-helper imports:
   `capture-json.ts` is currently inside `src`, so the library declaration build
   includes it under `rootDir: src`. The reusable ownership helper is under `tests`.
   Prefer a proper test-only location with updated callers over weakening compiler
   boundaries or duplicating ownership logic. Preserve captureStreams behavior.
4. Run the actual helper and six JSON-command suites, source/script typechecks and
   the relevant declaration-build check. Keep status/error/output behavior intact.
5. Reconcile all changed/retired paths in the inventory. Apply current per-file
   tier and decision checks; retain exact-source evidence and review limits.

Do not expand into unrelated `.deps.test.ts` helper repairs during this item.
Their existing ledger scopes remain separate.

Local implementation checkpoint: the helper and its tests now live at
`tests/helpers/capture-json{,.test}.ts`. All six callers use the owned tracker
and return its asynchronous cleanup hook. The database path remains absent
until the command creates it. Source, strict test types, declaration emission,
44 tests / 144 assertions, eight decision faults and combined failure reporting
pass locally. See `evidence/B11.4.json` for exact inputs and retained diagnostics.

Next: complete source-bound caller metric and optional-chain decision diagnostics.
Final B05 instrumentation admission, supported-platform proof and independent
review remain open. The default Bun report still excludes test helpers; it is
not coverage evidence for either moved file. Do not mark this item accepted from
the local diagnostic counters or transfer them to the complete package.
