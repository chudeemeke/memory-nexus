# B11.2 - Integration fixture disposition

Status: active after B11.1 local proof; independent acceptance remains separate.

`tests/integration/index.ts` opens databases and creates session directories,
silently catches cleanup failures, and exposes recursive deletion by arbitrary
path. Initial source searches found definitions but no uses of its named helpers.
Do not add an ownership registry to an unused module without proving a need.

1. Inventory every export and resolve source imports, including namespace and
   dynamic references, plus build/package inclusion. Retain source-bound results.
2. If the entire module is obsolete and unconsumed, remove it and reconcile the
   executable inventory and ledger with explicit removal evidence. Preserve Git
   recovery. Deleting dead destructive code is preferable to adding unused APIs.
3. If there are consumers, reproduce replacement-directory deletion and silent
   failure only in fresh owned synthetic fixtures. Adapt those consumers to the
   existing owned-directory/database capabilities; a supplied path alone must
   never authorize recursive deletion. Preserve real errors and retryability.
4. Verify the selected disposition through relevant type/import/build checks and
   actual affected callers. Do not run unrelated unsafe fixture suites or claim
   complete baseline acceptance from this local task.

B11.1 retains final Linux grouped execution (WSL startup failed), independent
review and final instrumenter/applicability acceptance. Those remain required;
starting B11.2 does not waive them or mark the native goal blocked/complete.
