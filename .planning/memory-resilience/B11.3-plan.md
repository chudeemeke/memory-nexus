# B11.3 - Observable UAT sandbox lifecycle

Status: active after B11.2 removal proof.

`scripts/run-uat-verification.ts` allocates and writes its sandbox before its
current try/finally, recursively removes by path, swallows removal errors and
unconditionally runs on import. Keep its installed-product acceptance separate
from synthetic lifecycle tests. Do not run the real UAT battery before isolation
and cleanup authority are established.

1. Read the complete call/setup/exit paths and existing tests. Add an import-safe
   entrypoint and a small lifecycle seam only where needed for real-process proof.
2. Reproduce failure reporting and replaced/foreign directory refusal in fresh
   synthetic fixtures. Reuse the existing owned-directory capability rather than
   authorizing deletion from the sandbox path string.
3. Enclose initialization, execution and teardown in the owned lifecycle. Retain
   the original acceptance failure, report cleanup failure and its retained path,
   and ensure failed cleanup cannot produce a successful final status.
4. Verify process exit, useful diagnostics, successful removal and retry/recovery.
   Cover initialization failure and a real/injected locked cleanup boundary.
5. Reconcile source inventory and applicable quality evidence. Preserve full UAT,
   installed artifact, complete per-file quality and independent-review gates.

B11.1 final Linux group and independent measurement/review acceptance remain open.
The native goal stays active while these external checks await readiness.

## Current checkpoint

The owned sandbox wrapper, import guard, result reporting and both projection
handle lifecycles are implemented. Nineteen focused Windows tests / 64 assertions
and source/script typechecks pass. Real children exercise passing, check failure,
setup failure, allocation failure, retained cleanup failure and an injected first
command refusal. SQLite tests use real in-memory handles, with injected open/close
failures where necessary. These checks are not installed-product acceptance.

A discarded direct-entrypoint test used a Bun preload module mock that did not
intercept the built-in spawn. The script attempted installed commands under its
synthetic HOME/XDG/MEMORY_HOME and failed on a missing synthetic event log. The
fixture was removed. The live database main-file timestamp preceded the run and
no WAL/SHM was present when checked; this is limited impact evidence, not proof
that no other side effect occurred. The replacement uses an explicit injected
runner and an isolated PATH/cwd. Retain the failed test log in checkpoint evidence.

Remaining acceptance work, owned by memory-nexus and resumed at the next B11.3 turn:

- Measure current script and regression driver with complete per-file metrics;
  use synthetic runner results for historical check branches, not installed CLI.
- Prove critical cleanup/close/status decisions reject faulty implementations.
- Exercise both actual replay call sites through synthetic workflow fixtures.
- Check command process lifetime (sequential stdout/stderr reads and no timeout
  remain in the existing runner) before admitting any installed UAT run.
- Complete supported-platform and independent review acceptance. Record all gaps;
  do not transfer helper-level proof to the whole script or product.
