---
context: audit
phase: null
task: Stage 1 next
total_stages: 4
status: paused
last_updated: 2026-05-11T14:47:39Z
---

# RESUME POINT — Architecture First-Principles Audit, post-Stage 0

> Paused after Stage 0 (provisional truths derivation) completed.
> Next action: **Stage 1 — spawn 4 CLI verification subagents in parallel + architecture-evidence pass.**

## Required Reading (in order, before doing anything)

1. **`docs/audits/2026-05-11-architecture-first-principles-audit.md`** — the audit doc itself. Read PART I §3 (execution stages), §6 (subagent specs A-D + 6.5 architecture-evidence pass), §16.0 (Stage 0 provisional truths — the lens for Stage 1).
2. **`.planning/audits/2026-05-11-architecture-first-principles-audit-plan.md`** — the durable schedule + scope artifact. §8 has the VERBATIM subagent brief framing that every Stage 1 subagent MUST receive.
3. **`.planning/reviews/2026-05-11-architecture-audit-phase-plan-codex-review.md`** — codex review #1 that produced the 8 revisions in PART I. Useful context for why the plan is shaped the way it is.
4. **`docs/inbox/2026-05-08-conversations-first-principles-architecture-audit.md`** — source inbox item (status: in-progress). The original ask + acceptance criteria.

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention |
|---|---|---|---|
| Anchoring on memory-nexus's current shape during truth derivation | Original plan let CLI surface frame the audit (subagents → adjacent → truths). Codex caught this. Stage 0 inverts the order. | blocking | Stage 1 subagents see Stage 0 truths as evaluation LENS, not as criteria to be derived FROM memory-nexus. Subagent briefs include the anti-bias note verbatim. |
| Adjacent-system research that compares against memory-nexus inside per-system writeups | Original plan §5.5 said adjacent research happens after CLI verification so each system's writeup could compare against current memory-nexus. Codex correctly identified this as backwards. | blocking | Per-system writeups (§5.3) are INDEPENDENT — no memory-nexus references inside. Comparison only happens in synthesis matrix §17. |
| Cross-AI review for every intermediate synthesis | Codex review cap is 2 calls (plan + recommendation). Intermediate subagent syntheses are NOT codex-reviewed. | blocking | §11 explicit cap. Internal review only for intermediate stages. |
| Pre-committing to Outcome C because it's the recorded prior | Prior toward "surgical consolidation" recorded in §10. Quarantined as "PRIOR, not recommendation." | advisory | If recommendation lands on C, held to HIGHER standard ("how do I know this isn't just following the prior?"). If anything else, that surprise is recorded as learning signal. |
| Including invented data for MemPalace | If no stable primary source found, record "no stable reference found" — don't invent comparison rows from secondary summaries. | blocking | §5.1 fallback rule explicit. |

## Current State

**Audit doc status:** in-progress (phase-level plan revised post codex review #1; Stage 0 complete).

**Stages:**
- ✅ Stage 0: Provisional truths (T1-T8) + constraints (C1-C3) + minimum structure derived. §16.0 filled.
- ⏭ Stage 1a: 4 CLI verification subagents (A friction / B search-context / C sync-ingestion / D admin) — NOT STARTED.
- ⏭ Stage 1b: Architecture-evidence pass — NOT STARTED.
- ⏭ Stage 2: Adjacent-system research (Hermes / OpenClaw / Mem0 / MemPalace) — NOT STARTED.
- ⏭ Stage 3: Synthesis (refine truths, fill matrix, gap analysis, decision rubric, recommend) — NOT STARTED.

**Cross-AI review budget:** 1 of 2 calls consumed (audit plan, BLOCK → resolved). 1 remaining, reserved for final recommendation review.

## Stage 1 Kickoff Instructions (next session's first action)

### Step 1: confirm Stage 0 is intact

```bash
grep -A2 "Stage 0 spawn gate cleared" docs/audits/2026-05-11-architecture-first-principles-audit.md
# Must see §16.0.7 confirming gate cleared
```

### Step 2: ask user to authorize subagent spawn

4 parallel subagents + 1 architecture-evidence pass = ~250-500k token total cost. Ask explicitly:

> "Stage 0 truths landed in §16.0. Next is Stage 1 — 4 parallel CLI verification subagents (friction / search-context / sync-ingestion / admin) plus architecture-evidence pass. Estimated 250-500k tokens. Authorize spawn?"

### Step 3: spawn subagents in ONE message with multiple Agent tool calls (parallel)

Each subagent brief MUST include verbatim:
- §0 user worry (load-bearing)
- Anti-bias note: "memory-nexus is not your sunk cost..."
- Cross-session context discipline: "Per `~/.claude/rules/subagent-trust-calibration.md`..."
- Stage 0 provisional truths T1-T8 (the evaluation lens)
- The subagent's scope (per §6.1-6.4)
- Output path (`docs/audits/2026-05-11-subagent-X-*.md`, <600 words)

For the architecture-evidence pass (§6.5): decide between 5th subagent (parallel) or main session (sequential after A-D). Decision factor: parallel cost vs session-budget.

### Step 4: synthesize Stage 1 outputs into §14 + §14.5

Each subagent's output linked from §14 or §14.5. NO premature recommendation; just evidence.

### Step 5: move to Stage 2 (adjacent-system research, main session)

Per §5.2: main session owns this, NOT subagents. Per §5.3: each system's writeup is independent of memory-nexus.

## Decisions Made (this session)

- **Audit kickoff was Stage 0 first, NOT subagent first.** Codex review #1 finding #1 forced this. Original plan was anchored on inspecting memory-nexus's surface before deriving truths.
- **Stage 0 produced 8 provisional truths.** T1-T8 in §16.0.3. NOT 5, NOT 12 — 8 felt like the floor without over-decomposing.
- **Provisional minimum structure is hybrid event-log + derived projection.** Considered 5 candidates including pure event log, schema DB, knowledge graph, vector-only, and hybrid. Hybrid satisfied T1-T8 + C1-C3 best.
- **Cross-repo doc fix is part of triage outcomes.** Friction-primacy disposition (previous session) included a `~/.claude/rules/tool-friction.md` edit. Established pattern: inbox triage CAN include cross-repo doc fixes; not every disposition is internal-only.
- **5 candidate outcomes, not 3.** Original ABC from inbox item expanded to ABCDE (D freeze at v4.0, E deprecate-or-replace) per codex pushback on the disposition. Excluding deprecation biases the audit.

## Infrastructure State

- Test-isolation arc closed (gate at 0; commits ee40d3f → 3ac29dc + earlier arc commits ).
- v4.0 Phase 32-37 PAUSED until audit recommendation lands.
- `.planning/STATE.md` milestone status: `gated_on_audit`.
- 4 inbox items currently open or in-progress: 3 self-filed (programmatic-api / friction.test.ts / Bun-Windows) + 1 architecture-audit (this work).
- The 3 self-filed inbox items are NOT architectural — they'll be triaged independently after audit closes, NOT folded into audit findings.

## Uncommitted Files (not part of pause; surfaced for awareness)

- `bun.lock`, `package.json` — pre-existing modifications unrelated to audit; from before test-isolation arc started.
- `.planning/reviews/2026-05-08-paths-ts-test-isolation-*.md` (2 files) — test-isolation arc planning docs that were never tracked. Should be added in a separate cleanup commit (non-blocking for audit).

## Resume Command

```bash
# At session start:
cd ~/Projects/memory-nexus
git log --oneline -8
cat .planning/.continue-here.md
cat .planning/HANDOFF.json | jq '.next_action, .stage_next'
```

Then read the Required Reading list above before any agent spawn.

## Cross-AI Review Discipline (carried forward)

The next external review call is the final recommendation §19, NOT Stage 1 outputs. Intermediate subagent outputs are internally reviewed only. This is per durable plan §9 cap and codex review #1 finding #5.
